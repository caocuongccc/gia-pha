{
  "scripts": {
    "build": "npx nx build frontend --configuration=production",
    "vercel-build": "../../node_modules/.bin/prisma generate --schema=../api/prisma/schema.prisma && npm run build"
  }
}